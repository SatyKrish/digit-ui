// API routes
export const API_ROUTES = {
  CHAT: "/api/chat",
  AUTH_CONFIG: "/api/auth/config", 
  AUTH_VALIDATE: "/api/auth/validate",
  HEALTH: "/api/health"
} as const

// Navigation routes
export const ROUTES = {
  HOME: "/",
  LOGIN: "/login"
} as const
